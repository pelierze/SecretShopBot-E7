# 이번 탐사 수집 결과 — 2026-09-25

## 실제 결과

기존 회차의 3층 심층 구역에서 재개했다. **클리어하지 못했다.** 새 이벤트 → 계단 이벤트 → 상점 2곳 → 보급 → 정예 전투를 진행했으나 비통의 수확자 케르 전투에서 패배했다. 패배 정산을 닫고 탐사 초기 화면으로 복귀했다.

마지막 갈림길 재검증: 도달 가능한 후보는 정예 2개였으며 일반 전투 후보는 없었다. 동일 종류의 위쪽 우선 규칙으로 위쪽 정예에 진입했다. 재현 화면은 `logs/chaos_clear_run/chaos_recruitment_e9o6bcsx/74_before_input.png`이다. 자동 전투 진행 신호는 정상 확인됐으며 정지·인식 실패가 아닌 게임 내 패배로 종료됐다.

실행 결과: `logs/chaos_clear_run/result.json`, `outcome=defeat`, `status=round_failed`. 재개 후 완료한 노드는 5개(배낭 이벤트, 계단 이벤트, 상점 2개, 보급), 입력은 25회였다. 최종 패배 화면은 `logs/chaos_clear_run/final_defeat.png`.

## 새 이벤트 1종 수집 및 등록

설명 기준 명칭: **주인 없는 배낭**. 공식 이벤트 제목은 화면에 표시되지 않았다.

- 보급품을 챙긴다: 모든 영웅 생명력 25% 감소, 탐험가의 나침반 획득 → 제외.
- 유해를 수습한다: 경험치 100 → 선택.
- 선택 후 결과 문구와 지도 복귀까지 실기 확인.

선택 화면 `images/chaos/node_progression/raw/event_abandoned_pack_live.png`, 결과 화면 `event_abandoned_pack_result_live.png`. `node_layout.json`에 선택·결과 식별자를 등록했다. 기존 3종을 포함해 등록 이벤트는 총 4종이다. 보고용 원본과 선택 메타데이터는 `logs/chaos_clear_run/unknown_events/20260925_134618_184200/`에 있다.

## 코드 보강 및 검증

돋보기 아이콘이 버튼 좌상단을 가리는 경우 기존 선택지 개수 인식이 실패했다. 좌하단 테두리와 우상단 테두리를 짝지어 개수를 확인하도록 변경해 2·3개 화면과 돋보기 변형을 검증했다. 제보용 저장 ON일 때 미등록 이벤트의 초기 인식 실패 화면도 저장하도록 보강했다. 기본 저장 OFF 동작은 유지한다.

노드·이벤트 테스트 49개 통과 후 저장 보강 관련 테스트 2개 추가 확인(1개 신규, 1개 기존 회귀). 상점 퇴장 확인과 보급 전리품 수령도 이번 실기에서 정상 진행됐다. 최종 보스 처치·클리어 후 처리 검증은 이번 패배로 수행하지 못했다.
